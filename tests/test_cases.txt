# Test Case 1: Valid Directory with .set Files
dataset_dir: src/data/alzheimer/clean
expected_behavior: Successfully load and combine all .set files and plot PSD.

# Test Case 2: Invalid Directory
dataset_dir: src/data/nonexistent_directory
expected_behavior: Raise ValueError with message "Not a .set file".

# Test Case 3: Directory with Mixed File Types
dataset_dir: src/data/mixed_files
expected_behavior: Only load .set files and ignore others, then combine and plot PSD.

# Test Case 4: Empty Directory
dataset_dir: src/data/empty
expected_behavior: Raise ValueError with message "Not a .set file".
